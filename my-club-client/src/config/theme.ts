const defaultPixelSize = 16;

const breakpoints = {
  xs: 30 * defaultPixelSize,
  sm: 48 * defaultPixelSize,
  md: 62 * defaultPixelSize,
  lg: 80 * defaultPixelSize,
  xl: 96 * defaultPixelSize,
};

export { breakpoints };
